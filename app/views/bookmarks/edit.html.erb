<div class="container">
  <h1><%= Movie.find(@bookmark.movie_id).title %></h1>
  <h1 class="banner-show" style="background-image: linear-gradient(90deg, rgba(0,0,0,1) 25%, rgba(255,255,255,0) 100%), url(<%= Movie.find(@bookmark.movie_id).poster_url %>)"><%= @list.name %></h1>
  <%= render "partials/bookmark-form" %>
</div>
