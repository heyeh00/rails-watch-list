<%= simple_form_for @bookmark[:list_id] = params[:list_id] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: @movies %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
